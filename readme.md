# jacq.cc hetzner cli

```bash
# Create a new volume and server
deno task create

# Delete an existing volume and server
deno task delete
```

Add an npm pacckage:

```bash
deno install -A -g -f npm:zx
```
